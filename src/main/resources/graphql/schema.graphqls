type Name{
    firstName: String!
    secondName: String!
}

type Author{
    id: ID
    name: String!
    email: String!
    posts: [Post]
    postsCount: Int
}

type Post{
    id: ID
    title: String
    content: String
}

type Comment {
    id: ID!
    text: String!
}